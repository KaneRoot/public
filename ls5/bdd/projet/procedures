-- a. Mettre à jour le compteur des miles d'un client donné en prenant en compte l'option de réduction de 100€ pour l'achat du prochain billet(i.e nb_miles = 1000).

create or replace procedure maj_miles( 
		ncompagnie COMPAGNIE.idCompagnie%TYPE,
		nclient CLIENT.idClient%TYPE)
IS
-- variables
nbMiles CARTE_FIDELITE.miles%TYPE;

BEGIN

SELECT miles INTO nbMiles FROM CARTE_FIDELITE WHERE idClient = nclient AND 

END;
